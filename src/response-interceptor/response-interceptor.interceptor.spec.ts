import { ResponseInterceptor } from './response-interceptor.interceptor';

describe('ResponseInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new ResponseInterceptor()).toBeDefined();
  });
});
